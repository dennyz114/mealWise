# Dark mode

- Auto-detect system preference on first visit.
- Manual toggle (profile dropdown / theme control) overrides system.
- Preference persists (localStorage); once set manually, system changes do not override it.
- Light and dark themes must work across all screens (design tokens in `docs/STYLING.md`).
